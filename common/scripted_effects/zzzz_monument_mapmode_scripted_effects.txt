generate_monument_mapmode_states_array = {
	# for compatibility
}

monument_mapmode_set_hashed_icon_frame = {
	# for compatibility
}

update_monument_mapmode = {
	# for compatibility
}

open_monument_map_mode_effect = {
	# for compatibility
}

close_monument_map_mode_effect = {
	# for compatibility
}